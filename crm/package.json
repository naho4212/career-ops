{
  "name": "career-ops-crm",
  "private": true,
  "description": "Local web CRM overlay for career-ops. Own package so the root package.json (a system-layer file) stays untouched.",
  "type": "module",
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  },
  "scripts": {
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/*/spawn-helper 2>/dev/null || true"
  }
}
